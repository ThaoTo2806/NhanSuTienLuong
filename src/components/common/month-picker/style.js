import { StyleSheet } from 'react-native';

export const styles = StyleSheet.create({
  wrapper: {
    alignItems: 'center',
    flexDirection: 'row',
    marginRight: 15,
  },
  icon: {
    marginLeft: 12,
  },
  prevMonth: {
    marginRight: 15,
  },
});
